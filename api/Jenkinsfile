pipeline {
    agent any

    stages {
        stage('Building/Deploying') {
            steps {
                withDockerRegistry(credentialsId: '80f85ebb-111e-4b14-b4d5-36745824549d', url: 'https://index.docker.io/v1/') {
                    sh 'docker-compose up -d --build'
                    sh 'docker-compose push'
                }
            }
        }
    }

    post {
        always {
            cleanWs()
        }
    }
}
