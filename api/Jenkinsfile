pipeline {
    agent any

    stages {
        stage('Building/Deploying') {
            steps {
                withDockerRegistry(credentialsId: 'a3d3001f-5924-4430-8067-05118880c0f4', url: 'https://index.docker.io/v1/') {
                    sh 'docker-compose up -d --build'
                    sh 'docker-compose push'
                }
            }
        }
    }

    post {
        always {
            cleanWs()
        }
    }
}
